/**
 * Description: Non injectable environment variales
 */

export const {
  NODE_ENV,
  CRYPTO_SECRET,
  TSI_MYSQL_USER,
  TSI_MYSQL_PASSWORD,
  TSI_MYSQL_DATABASE,
  TSI_MYSQL_HOST,
  TSI_MYSQL_PORT,
  TSI_MYSQL_EXTERNAL_PORT,
} = process.env;
